-change friends JSON to match user JSON (make both the same) and add facebook id
["name": "Rok Kralj","facebookID": "FACEBOOK_ID", "total": 311.0021136042848, ....

-all returned integers to .2 fixed decimal point => 2.0902328983200 -> 2.09

-fix top lestvice

-display all friends, those that don't already use our app show invite button

-add two fields to user database:
	friends_only: boolean (default: false)
	public: boolean (default: true)